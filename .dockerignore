# Never bake secrets into an image.
.env
.env.*
!.env.example

node_modules
dist

.git
.github
.vscode
.idea

# Local-only tooling and docs.
docker-compose.yml
dev.sh
README.md
CLAUDE.md
LICENSE

# Tests don't ship; the runtime image has no dev dependencies to run them with.
**/*.test.ts
src/test

*.log
.DS_Store
